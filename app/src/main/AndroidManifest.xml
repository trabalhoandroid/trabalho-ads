<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:ferramentas="http://schemas.android.com/tools"
          package="com.example.ednaldojunior89.resoftsystemas">

    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_ico"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".Main2Activity_mesas"/>
        <activity android:name=".Main2Activity_scanner"/>
        <activity android:name=".Main2Activity_cardapio"/>
        <activity android:name=".cadastro_pratos"/>
        <activity android:name=".AdapterListView">
        </activity>
        <activity android:name=".Comanda">
        </activity>
        <activity android:name=".cadastro">
        </activity>
    </application>

</manifest>